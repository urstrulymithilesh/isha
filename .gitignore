# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Esha runtime data (local, private — never commit)
*.db
*.sqlite
*.sqlite3
data/
models/
*.wav
*.onnx
.env

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# The remote-access token. Covered by data/ already; named explicitly because a
# leaked secret is not the place to rely on an inherited rule.
data/remote-token.txt
*remote-token*
